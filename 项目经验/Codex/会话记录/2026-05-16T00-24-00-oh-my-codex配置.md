# 2026-05-16 oh-my-codex 配置

## 项目目标

按 GitHub 上 `Yeachan-Heo/oh-my-codex` 的当前发布方式，为本机 Codex 配置 oh-my-codex/OMX。

## 路径

- 工作目录：`C:\Users\fkl26\Documents\Codex\2026-05-16\codex-github-ohmycodex`
- Codex Home：`C:\Users\fkl26\.codex`
- OMX npm 全局安装位置：`D:\DevHome\npm-global\node_modules\oh-my-codex`

## 关键决策

- 使用 `oh-my-codex@0.17.3`，全局 npm 安装后通过 `omx setup` 配置。
- 使用插件模式：`omx setup --scope user --plugin --with-mcp --merge-agents --verbose`。
- 保留现有 `C:\Users\fkl26\.codex\AGENTS.md`，未用 OMX 默认 AGENTS 覆盖本地规则。
- 配置前备份了 `config.toml`、`hooks.json`、`AGENTS.md`、`skills` 等关键内容到 `C:\Users\fkl26\.codex\backups\oh-my-codex-20260516-001617`。
- OMX 自身也写入了备份：`C:\Users\fkl26\.omx\backups\setup\2026-05-15T16-17-16.029Z`。

## 实施内容

- 安装 `oh-my-codex` npm 包，提供 `omx` 命令。
- 注册本地 Codex 插件 marketplace：`oh-my-codex-local`。
- 写入/更新 `C:\Users\fkl26\.codex\hooks.json`，加入 OMX native hook shim，同时保留原有 GitNexus、SessionStart、Obsidian Stop hook。
- 更新 `C:\Users\fkl26\.codex\config.toml`：
  - 开启 `features.hooks = true`
  - 开启 `features.goals = true`
  - 保持 `features.multi_agent = true`
  - 启用 `plugins."oh-my-codex@oh-my-codex-local"`
  - 注册 6 个 OMX MCP 兼容服务：state、memory、code-intel、trace、wiki、hermes
- 修复一次 TOML 解析失败：OMX 写入的新 hook trust state 与旧单引号路径 trust state 语义重复，删除旧的 hook trust state 表后 `omx doctor` 通过。

## 验证结果

- `omx --version`：`oh-my-codex v0.17.3`
- `omx doctor`：13 passed，0 failed，2 warnings。
- `omx doctor --team`：team diagnostics passed。
- `codex features list` 能正常读取配置，`multi_agent`、`plugins`、`memories` 为开启状态。
- `omx list --json` 能列出 47 个 OMX 技能、30 个 agent/prompt 表面。

## 已知风险

- Windows 上 OMX 内置 explore harness 依赖 POSIX sh/bash wrapper，诊断提示不可用；优先使用 `omx sparkshell` 做 shell-native 只读查询。
- `C:\Users\fkl26\.agents\skills` 与 `C:\Users\fkl26\.codex\skills` 有 8 个重名技能且内容不同：`archive-content-curator`、`gitnexus-cli`、`gitnexus-debugging`、`gitnexus-exploring`、`gitnexus-guide`、`gitnexus-impact-analysis`、`gitnexus-pr-review`、`gitnexus-refactoring`。本次未清理，避免破坏旧 `.agents` 环境。

## 后续动作

- 日常可用 `omx doctor` 检查健康状态。
- 需要更新 OMX 时使用 `omx update`，更新后重新跑 `omx doctor`。
- 若确认不再需要旧 `.agents` 技能根，可另开任务对比并归档重复技能。

## 2026-05-16 00:46 追加：对话优先万能 Codex 配置

### 目标

用户希望把 Codex 当成“小白对话模式”使用：只描述目标，不需要理解模型、MCP、技能、插件、验证流程等内部细节；Codex 自动调研、选工具、执行、验证和总结。

### 官方调研依据

- OpenAI Codex best practices：强调清晰目标、上下文、完成标准、迭代验证。
- OpenAI Codex customization：强调用 `AGENTS.md` 固化仓库/环境规则，用 MCP 与工具扩展上下文和能力。
- Codex CLI/CI 相关帮助文档：强调让 Codex 运行检查、给出可复现结果，并把环境配置保持明确。

### 配置决策

- 不改模型供应商、模型名、API Key、Base URL、认证、计费或网关配置；用户明确要求“不要动模型 api 接口”。
- 将全局行为规则改成“conversation-first”：用户自然语言描述目标即可，Codex 默认补上下文、选工具、执行、验证、总结。
- 只有涉及数据丢失、花钱、安全暴露、账号变化、模型/API 接口变化或重大工作流偏移时，才向用户提一个短问题。
- 涉及 Codex、ChatGPT、OpenAI API、模型、提示词或 Agents SDK 时，优先使用 OpenAI 官方文档。
- 新增官方文档 MCP：`openaiDeveloperDocs`，地址 `https://developers.openai.com/mcp`。
- 将 `.agents/skills` 中仍有用的 45 个旧技能迁移到 canonical `C:\Users\fkl26\.codex\skills`。
- 将旧 `.agents/skills` 根归档到 `C:\Users\fkl26\.agents-archive\skills-active-root-20260516-0049\skills`，避免重复/旧根警告。
- 先前 8 个重名旧技能已归档到 `C:\Users\fkl26\.agents\skills-archive\duplicates-20260516-0046`。

### 修改文件

- `C:\Users\fkl26\.codex\AGENTS.md`
- `C:\Users\fkl26\假桌面\AI_Commander_Docs\reference\governance\codex-general-operating-rules.md`
- `D:\Obsidian\初始\99 - Agent指南与规范\Codex-轻量通用规则-2026-04-24.md`
- `C:\Users\fkl26\.codex\skills\codex-local-operating-norms\SKILL.md`
- `C:\Users\fkl26\.codex\config.toml` 仅由 `codex mcp add openaiDeveloperDocs --url https://developers.openai.com/mcp` 增加官方文档 MCP；未改模型/API 接口段。

### 备份

- 本轮备份：`C:\Users\fkl26\.codex\backups\conversation-first-codex-20260516-004601`
- OMX 安装备份：`C:\Users\fkl26\.codex\backups\oh-my-codex-20260516-001617`

### 验证结果

- `omx doctor`：14 passed，0 failed，1 warning。
- 剩余 warning：Windows 上 OMX built-in explore harness 依赖 POSIX sh/bash wrapper；建议使用 `omx sparkshell` 或未来设置兼容 harness。
- `codex mcp get openaiDeveloperDocs`：enabled，transport 为 streamable_http。
- `.agents/skills` 已不存在；`.codex/skills` 当前 120 个技能目录；旧根已归档。
- `codex features list`：`memories`、`multi_agent`、`plugins`、`skill_mcp_dependency_install`、`tool_search` 为可用/开启状态。

### 已知风险

- 当前会话的技能列表不会实时反映刚迁移后的根目录变化；重启 Codex 后会加载新的 canonical skill 根。
- OMX 的 Windows explore harness 警告不是配置错误，而是平台限制；使用 `omx sparkshell` 更稳。
- `.agents\.skill-lock.json` 保留未动，作为旧 skill installer 的来源记录；未来若再次用旧 installer，可能重新创建 `.agents/skills`。

### 下一步

- 重启 Codex，让新 MCP 和迁移后的 skill 根在下一会话完整生效。
- 后续若再出现 `.agents/skills`，优先把新增技能迁移到 `.codex/skills`，再归档旧根。
- 需要继续增强时，优先做工具可用性验证和少量高价值 workflow skill，不盲目增加插件。

## 2026-05-16 追加：Windows 最佳实践基线

### 用户约束

- 用户追问是否按 Windows 最佳实践配置完成。
- 用户明确要求不要动模型/API 接口。

### 调研依据

- OpenAI Codex Windows 文档：原生 Windows 推荐使用 elevated sandbox；WSL2 适合代码和工具链都在 Linux 环境的场景。
- OMX doctor 当前提示：Windows 上 built-in explore harness 依赖 POSIX sh/bash wrapper，不是本机配置错误。

### 配置决策

- 保留现有 `[windows].sandbox = "elevated"`，不改 shell、PATH、PowerShell profile、terminal、模型、provider、API Key、Base URL。
- 将 Windows 默认行为写入以下规则面：
  - `C:\Users\fkl26\.codex\AGENTS.md`
  - `C:\Users\fkl26\假桌面\AI_Commander_Docs\reference\governance\codex-general-operating-rules.md`
  - `D:\Obsidian\初始\99 - Agent指南与规范\Codex-轻量通用规则-2026-04-24.md`
  - `C:\Users\fkl26\.codex\skills\codex-local-operating-norms\SKILL.md`
- Windows 默认行为：
  - 原生 Windows 优先，不默认假设 WSL/Git Bash。
  - 优先 PowerShell、Windows 绝对路径和 Windows 安全引号。
  - 不默认依赖 `sh`、`bash`、`sed`、`awk`、`xargs`、`/tmp`、可执行位或 POSIX symlink 语义。
  - 搜索优先 `rg`，文件操作优先 PowerShell cmdlet。
  - 递归移动/删除前验证解析后的绝对路径。
  - `cmd /c` 只用于 batch/npm shim 兼容。
  - OMX explore 受限时，优先 native Codex tools、`rg`、PowerShell inspection 或 `omx sparkshell`。

### 验证结果

- `omx doctor`：14 passed，0 failed，1 warning。
- `codex mcp get openaiDeveloperDocs`：官方 OpenAI docs MCP 已启用。
- `codex features list`：`memories`、`multi_agent`、`plugins`、`skill_mcp_dependency_install`、`tool_search` 保持启用。
- `config.toml` 中模型/API 接口仍为原状态；未修改模型名、provider、base_url 或 env_key。

## 2026-05-16 追加：用户明确不用沙箱

### 用户约束

- 用户表示“不用沙箱”。
- 继续保留“不动模型/API 接口”的约束。

### 配置变更

- 在 `C:\Users\fkl26\.codex\config.toml` 顶层加入：
  - `sandbox_mode = "danger-full-access"`
  - 原有 `approval_policy = "never"` 保持不变。
- 将“不重新开启沙箱，除非用户明确要求”写入：
  - `C:\Users\fkl26\.codex\AGENTS.md`
  - `C:\Users\fkl26\假桌面\AI_Commander_Docs\reference\governance\codex-general-operating-rules.md`
  - `D:\Obsidian\初始\99 - Agent指南与规范\Codex-轻量通用规则-2026-04-24.md`
  - `C:\Users\fkl26\.codex\skills\codex-local-operating-norms\SKILL.md`

### 备份

- 本轮备份：`C:\Users\fkl26\.codex\backups\no-sandbox-20260516-005544`

### 验证结果

- `omx doctor`：14 passed，0 failed，1 warning。
- `config.toml` 顶层已显示 `sandbox_mode = "danger-full-access"`。
- 模型/API 接口未变：`model = "gpt-5.5"`、`model_provider = "myproxy"`、`base_url = "https://an520.xin/v1"`、`env_key = "OPENAI_API_KEY"` 仍为原值。

### 风险

- 无沙箱意味着后续 Codex 命令默认拥有更高本机写权限。
- 已在规则中保留路径验证要求：即使无沙箱，递归删除/移动等破坏性操作前仍必须验证解析后的绝对路径。
